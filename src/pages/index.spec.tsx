import { render } from '@testing-library/react'
import IndexPage from './index'

describe('IndexPage switch tests', () => {
  test('Home load', async () => {
    // const { getByText } = render(IndexPage)
    console.log(process.env)

    expect(true).toBe(true)
  })
})
